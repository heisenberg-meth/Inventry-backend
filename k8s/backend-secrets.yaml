apiVersion: v1
kind: Secret
metadata:
  name: ims-secrets
  namespace: ims-namespace
type: Opaque
stringData:
  # DO NOT COMMIT REAL SECRETS HERE. 
  # Use Helm '--set' during deployment or K8s External Secrets.
  DB_PASSWORD: "REPLACE_ME"
  REDIS_PASSWORD: "REPLACE_ME"
  JWT_SECRET: "REPLACE_ME"
  APP_ENCRYPTION_KEY: "REPLACE_ME"
