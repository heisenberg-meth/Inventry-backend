apiVersion: apps/v1
kind: Deployment
metadata:
  name: ims-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ims-redis
  template:
    metadata:
      labels:
        app: ims-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "200m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: ims-redis
spec:
  selector:
    app: ims-redis
  ports:
    - port: 6379
      targetPort: 6379
  type: ClusterIP
