spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password: 
    hikari:
      maximum-pool-size: 5
      minimum-idle: 0
      idle-timeout: 10000
      max-lifetime: 30000
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    database-platform: org.hibernate.dialect.H2Dialect
    properties:
      hibernate:
        # multiTenancy: NONE
  cache:
    type: none
  flyway:
    enabled: true
  data:
    redis:
      host: localhost
      port: 6379

app:
  jwt:
    secret: test-only-secret-do-not-use-in-production-000000000000000000000000
    expiry-seconds: 86400
    refresh-expiry-seconds: 604800
  cache:
    product-ttl-minutes: 15
    stock-ttl-minutes: 5
    report-ttl-minutes: 30
    tenant-ttl-hours: 1
  rate-limit:
    auth-rpm: 20
    public-rpm: 50
    authenticated-rpm: 200
    window-seconds: 60
  security:
    encryption-key: "test-encryption-key-32-bytes-len"

management:
  health:
    redis:
      enabled: false
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    org.springframework: DEBUG
